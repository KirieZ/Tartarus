Import folder allows you to store your settings without having problems
with updates.

It's generated the first time you Build/Rebuild Tartarus, DON'T change
import-tmpl files.

import folder is not included in your commits, so you can keep your
changes safe there.